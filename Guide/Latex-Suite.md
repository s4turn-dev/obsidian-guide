Obsidian умеет рендерить LaTeX-формулы "из коробки", но плагин **"Latex Suite"** значительно ускоряет и упрощает их ввод благодаря автодополнению и сниппетам.

>[!warning] Внимание
> Эта статья предполагает, что ты уже знаешь [[Плагины-для-Obsidian|как устанавливать сторонние плагины]].

## Как работает Latex Suite?

Плагин мониторит, какие символы ты вводишь с клавиатуры, и встречая определенные заранее комбинации символов – подставляет соответствующие им конструкции LaTeX. Мы будем называть этот функционал *макросами*.


## Ввод формул

- Введи `mk` – и Latex Suite автоматически окружит твой курсор одинарными `$`
- Введи `dm` для подстановки двойных `$$` (блочная формула)
- Используй `Tab` для переноса курсора в "следующую" секцию формулы (например, из числителя дроби `\frac{}{}` в знаменатель. или для выхода из формулы, если курсор в ее конце)

Попробуй набрать на клавиатуре следующий текст, расположив курсор в Обсидиане (не забудь предварительно [[Плагины-для-Obsidian|установить плагин]] Latex Suite):

>`Площадь круга mdS<TAB> вычисляется как mdpirsr<TAB>`

## Базовые приемы

### Степени и индексы

| Описание                             | Макрос              | Что подставится           |
| ------------------------------------ | ------------------- | ------------------------- |
| Квадрат                              | `sr`                | `^{2}`                    |
| Куб                                  | `cb`                | `^{3}`                    |
| Произвольная степень                 | `rd`                | `^{}`                     |
| Произвольный индекс                  | `_`                 | `_{}`                     |
| Нижний числовой индекс 0-9           | Цифра после символа | Пример: `z1` --> `z_{1}`  |
| Индексы `n`, `i` и `j` для `x` и `y` | `xnn`, `yii`, `xjj` | `x_{n}`, `y_{i}`, `x_{j}` |
| Степень для `e`                      | `ee`                | `e^{}`                    |

### Дроби и корни

- Пустая дробь: `//` --> `\frac{}{}`.
- Квадратный корень: `sq` --> `\sqrt{}`.

>[!tip] Совет
> Используй `Tab` для продвижения курсора "вперед" по формуле.

Пример: `//a/b<Tab><Tab>c/d<Tab><Tab>` --> $\frac{\frac{a}{b}}{\frac{c}{d}}$

Дроби можно инициировать и по-другому: если после неразрывной последовательности*  поставить `/`, то Latex Suite подставит ее всю в числитель, а курсор в знаменатель. Пример: `bsr/4ac` --> `\frac{b^{2}}{4ac}`

>[!tip]- Корень n-ной степени
>Задается как `\sqrt[n]{}`, но макроса для него мы не знаем.

\* -- последовательности символов, не разделенной пробелами, знаками арифметических операций или другими символами LaTeX, которые Latex Suite воспринимает как "разделители"

### Греческие буквы

Буквы, для которых определены и заглавные, и строчные написания, отличаются капитализацией первой буквы их конструкции: `\theta` и `\Theta` --> $\theta$ и $\Theta$.  
  
Для большинства букв греческого алфавита предопределен макрос, позволяющий набирать их как `@X`, где `X` – первая буква конструкции:

| Макрос | Что подставится | Результат |
| ---------- | --------------- | --------- |
| `@a`       | `\alpha`        | $\alpha$  |
| `@b`       | `\beta`         | $\beta$   |
| `@g`       | `\gamma`        | $\gamma$  |
| `@o`       | `\omega`        | $\omega$  |
| `@G`       | `\Gamma`        | $\Gamma$  |
| `@D`       | `\Delta`        | $\Delta$  |
| `@O`       | `\Omega`        | $\Omega$  |

Макросы некоторых греческих букв – их названия:

| Макрос | Что подставится | Результат |
| ---------- | --------------- | --------- |
| `pi`       | `\pi`           | $\pi$     |
| `phi`      | `\phi`          | $\phi$    |
| `rho`      | `\rho`          | $\rho$    |

### Матрицы и системы уравнений

В общем виде синтаксис многострочных выражений, будь то матрицы или что угодно другое, выглядит так:
```latex
\begin{TYPE}
...
\end{TYPE}
```

Эту структуру вставит макрос `beg` из стандартного конфига.

Здесь `TYPE` – "тип" многострочного выражения. Единственное, на что он влияет – ограничители (скобки) вокруг выражения:

- `pmatrix`: `()` – классическая матрица
- `vmatrix`: `||` – определитель
- `cases`: `{` – система
- `bmatrix`: `[]`
- `array`: ` `

Для каждого типа предопределен макрос:

| Макрос | Что подставится      |
| ---------- | -------------------- |
| `pmat`     | `\begin{pmatrix}...` |
| `vmat`     | `\begin{vmatrix}...` |
| `bmat`     | `\begin{bmatrix}...` |
| `array`    | `\begin{array}...`   |
| `cases`    | `\begin{cases}...`   |

Для разделения столбцов и строк матриц и систем в LaTeX предусмотрен следующий синтаксис:
- `&` разделяет столбцы (нажатие `Tab`)
- `\\` разделяет строки (нажатие `Enter`)
Пример: `dmA=pmat1<Tab>2<Enter>3<Tab>4` --> $$
A=\begin{pmatrix}
1 & 2 \\
3 & 4
\end{pmatrix}
$$

Ты можешь описать матрицу в режиме строчной формулы, но помни, что все должно быть написано в одну строку, без переносов.

Пример: `mk@D=vmat1<Tab>0<Tab>0\\0<Tab>1<Tab>0\\0<Tab>0<Tab>1` --> $\Delta=\begin{vmatrix}1 & 0 & 0\\0 & 1 & 0\\0 & 0 & 1\end{vmatrix}$

### Сумма, предел, интегралы

Для записи суммы, предела и интеграла есть свои символы:

| Макрос     | Что подставится         | Результат             | Комментарий                       |
| ---------- | ----------------------- | --------------------- | --------------------------------- |
| `lim`      | `\lim_{ n \to \infty }` | $\lim_{n \to \infty}$ | Шаблон для предела                |
| `sum<Tab>` | `$\sum_{i=1}^{N}$`      | $\sum_{i=1}^{N}$      | Сумма с пределами                 |
| `int<Tab>` | `\int  \, dx`           | $\int  \, dx$         | Неопределенный интеграл           |
| `dint`     | `\int_{0}^{1} \, dx`    | $\int_{0}^{1}  \, dx$ | Определенный интеграл с пределами |

Некоторые фрагменты будут выделены, курсор подставится перед первым из них -- это позволяет, не производя дополнительных действий, редактировать их и перемещаться из одного в следующий по нажатию `<Tab>`. Звучит непонятно, поэтому примеры:

- Интеграл от t по dt: `mkint<Tab>t<Tab>t<Tab><Tab>` --> $\int t \, dt$
- Интеграл от 0 до П от синуса y/2 по dy: `mkdint0<Tab>pi<Tab>siny/2<Tab>y<Tab><Tab>` --> $\int_{\pi}^{\pi/2} \sin \frac{y}{2} \, dy$
- Сумма x<sub>i</sub> по i от 0 до n: `mksum<Tab><Tab>0<Tab>n<Tab>xii<Tab>` --> $\sum_{i=0}^{n}x_{i}$

Молниеносно и удобно!

### Специальные обозначения

Этот раздел содержит наиболее часто встречающиеся символы и операции.

#### Символы и операции

| Что ввести       | Что подставится | Результат           |
| :--------------- | :-------------- | :------------------ |
| `ooo`<br>`infty` | `\infty`        | $\infty$            |
| `+-`             | `\pm`           | $\pm$               |
| `**`             | `\cdot`         | $\cdot$ (умножение) |
| `xx`             | `\times`        | $\times$            |
| `exists`         | `\exists`       | $\exists$           |
| `forall`         | `\forall`       | $\forall$           |
| `>=`             | `\geq`          | $\geq$              |
| `<=`             | `\leq`          | $\leq$              |
#### Отношения и стрелки

| Что ввести       | Что подставится   | Результат                        |
| :--------------- | :---------------- | :------------------------------- |
| `!=`             | `\neq`            | $\neq$                           |
| `->`             | `\to`             | $\to$ (до, стремится к)          |
| `<->`            | `\leftrightarrow` | $\leftrightarrow$                |
| `\Leftrigharrow` |                   | $\Leftrightarrow$ (эквивалентно) |
| `=>`             | `\implies`        | $\implies$ (следовательно)       |
| `inn`            | `\in`             | $\in$ (принадлежит)              |
| `notin`          | `\not\in`         | $\not\in$ (не принадлежит)       |
| `sub=`           | `\subseteq`       | $\subseteq$                      |
| `simm`           | `\sim`            | $\sim$                           |
| `approx`         | `\approx`         | $\approx$                        |

#### "Шапочки"

| Что ввести   | Что подставится | Результат  |
| ------------ | --------------- | ---------- |
| `xvec`       | `\vec{x}`       | $\vec{x}$  |
| `vecab<Tab>` | `\vec{ab}`      | $\vec{ab}$ |
| `Abar`       | `\bar{A}`       | $\bar{A}$  |
| `barXY<Tab>` | `\bar{XY}`      | $\bar{XY}$ |

#### Шрифты для множеств и форматирование

| Что ввести | Что подставится | Результат     | Описание                                  |
| :--------- | :-------------- | :------------ | :---------------------------------------- |
| `RR`       | `\mathbb{R}`    | $\mathbb{R}$  | Множество действительных чисел            |
| `CC`       | `\mathbb{C}`    | $\mathbb{C}$  | Множество комплексных чисел               |
| `LL`       | `\mathcal{L}`   | $\mathcal{L}$ | Каллиграфический шрифт (Лагранжиан)       |
| `bf`       | `\mathbf{}`     | $\mathbf{}$   | Жирный шрифт (для векторов/матриц)        |
| `rm`       | `\mathrm{}`     | $\mathrm{}$   | Прямой шрифт (для единиц измерений, `dx`) |


## Что дальше?

**Практика – залог успеха.** Основные макросы легко запоминаются при постоянном использовании. Как только ты начнешь писать формулы с их помощью, твоя скорость ввода навсегда увеличится.

В данной статье мы указали символы и макросы, которыми пользовались чаще всего. Если тебе встретится что-то незнакомое, проверь конфиг Latex Suite в настройках плагинов Obsidian – скорее всего там уже есть то, что ты ищешь. А если нет или хочется сделать по-своему – ты всегда можешь отредактировать конфиг под свое удобство и свои нужды.

## Обратная связь

Чего-то не хватает? Что-то было непонятно? Остались вопросы?

- Спроси в [чате](https://t.me/+rLsgrAwi_b1mYzli),
- или можешь [открыть issue](https://github.com/s4turn-dev/obsidian-guide/issues/new) со своим вопросом.
